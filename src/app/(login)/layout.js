/**
 * Brief: Layout simple pour les pages de connexion sans header/footer
 * @param {Object} props - Propriétés du composant
 * @param {React.ReactNode} props.children - Pages enfants à afficher directement
 * @returns {React.ReactNode} Contenu des pages de login sans wrapper supplémentaire
 */
export default function LoginLayout({ children }) {
  return  children
}
